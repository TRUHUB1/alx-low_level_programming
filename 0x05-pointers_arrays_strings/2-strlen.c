#include "main.h"

/**
* _strlen - writes a function that returns length of a string.
*
* @s: variable pointer at s
*
*Return: count
*/

int _strlen(char *s)
{
int count;

for (count = 0; s[count] != '\0'; count++);
{

}
return (count);
};
